#include <iostream>
#include "AI_LandBase.hpp"
#include "Enemy.hpp"

AI_LandBase::AI_LandBase(Enemy* enemy) : AI(enemy)
{}
void AI_LandBase::move()
{
	//_enemy->getShape();
	//_enemy.getShape().setPosition(_enemy.getShape().getPosition().x + 1, _enemy.getShape().getPosition().y);
}